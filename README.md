### python:
# functions(2nd.py):
1)What is a function?
A block of reusable code.

2)Why are functions used?
To avoid repetition and improve readability.

3)Difference between function definition and function call?
Definition creates it, call runs it.

4)What are parameters and arguments?
Parameters receive values; arguments are the values passed.

5)What is return?
Sends a value back from a function. 
example: def greet():
              print("hello")
         greet()     

# Lists:
1) why we use lists
it stores multiple values in single variable,
easy processing usng loops 
used in DSA,AIML,web devlomnt,data science
2)indexing,slicing,append(),remove(),insert()
3)for finding second highest,3rd,etc we use sort() function and -ve indexing 
4) count, total,for loop, used to find counting.....

# strings: 
strings is a simply text ex;name="yallappa"
indexing,slicing,name.upper(),name.lower(),len(),name.capatalize()
replace(), endswith(), startswith(), 
checking if a word is present in srting...

# Dictionaries: 
--dictionary is important concept used for storing the marks of the students and various things
in this key : value are used....
--while storing the integers we must not use the "", because it becomes the string.. we we have to store without double cte.....
--lerant loops with dictionaries.. #04 question...
--using loops,list,dictionaries we find topper,low scorer,avg marks.....
--finding the student marks by name or by marks....
--finding distinctions students and printing names and marks both....
--Upadating marks of a student, and deleting student....
--key()
--Values()
--get()

# File handling....
--Write 'w' writes in the file and if old content is there it deletes the content...
--read 'r' only reads the content and prints that if we want that, nothing deleted....
-- append 'a' adds the element at last without deleting any thing....
--readline(), readlines(), write for multple lines...
--for count lines len()...
-- for data in one line for counting the length its takes every characters...

# Exception handling....
try: means try this code...
except: if an error occurs then run this block...
in this try: runs first if error occurs then except...
for taking int input if user entrs abc then prog crashes.... if we use the exception then it will not crash.....

# OOP (objective-oriented prog)....
 learnt class, object, attrubute, pass,
 __init__():
 --methods {display()} prints data ....
-- by using the loops we can store 100 students details by giving range in loop...
 
---encapsulation...
  it protects the data...
  set_marks(for stu marks) : validates the marks before updating
---inheritence....
-- inheritance pillar of OOP : it inherits the data from the parent class to child class...
for example a class person as age and name ... student class also inherits the same things ...
super(). __init__: is used to inherit the parent class data .....

--- POLYMORPHISM....
Poly means many and morph means forms.....
same thing which as different outputs for example all animals and birds as sound as common but noise different like bark moo , kaaa ect....
---METHOD OVERRIDING---
parental class is overrinding by child class
super(): this method in overrinding will help to print both the parental class and child class
--- METHOD OVERLoADING....
method overloading means using the same method with different numbers of arguments
for example: a,b=0,c=0
---ABSTRACTION...
means hiding the implementaion details and only showing the imopratant details or features...

---Multiple Inheritance...
Multiple inheritance ia a type inheritance where one child class inherits the properties and methods fron two or more parent classes...

---Multilevel inheritance....
multilevel inheritance is a type of inheritance whre child class inherits from another child class, forming chain of inheritance
for example inheriting the land from grandfather and house from father to son...

---Hierarchial inheritance....
is type of inheritance where one parent class is inherited by one or more child class.... 
